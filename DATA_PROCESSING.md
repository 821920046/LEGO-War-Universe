# Data processing

Local planning runs in the browser. The default build does not send a theme or asset catalogue to a model service. If an operator enables the protected Pages endpoint, only allow-listed fields are processed, production requests require authentication, and no API key is sent to the browser.
